/**
 * Testing file that was created to not get an error when running `deno test`.
 * Remove this dummy test file when real tests are added.
 */

import { assertEquals } from "testing";

Deno.test("2 + 2 = 4", () => {
  assertEquals(2 + 2, 4);
});
